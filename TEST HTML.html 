<!-- app/templates/event_details.html -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ event.name }} - CityPulse</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <header>
        <h1>CityPulse - Event Details</h1>
        <nav>
            <a href="/">Home</a> |
            <a href="/events">Events</a>
        </nav>
    </header>

    <main>
        <div class="event-details">
            <h2>{{ event.name }}</h2>
            <p><strong>Description:</strong> {{ event.description }}</p>
            <p><strong>Location:</strong> {{ event.location }}</p>
            <p><strong>Date:</strong> {{ event.start }}</p>
            <p><strong>Category:</strong> {{ event.category }}</p>
            <p><strong>Ticket Link:</strong> <a href="{{ event.url }}" target="_blank">Buy Tickets</a></p>
        </div>
    </main>

    <footer>
        <p>&copy; 2025 CityPulse. All rights reserved.</p>
    </footer>
</body>
</html>
